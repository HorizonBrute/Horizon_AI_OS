# Local Agent Teams (.claude scope) — machine-local override (gitignored; never synced or clobbered)

<!--
This is the TEMPLATE for the .claude/ scope. `aios setup` materializes it to
`local.agent_teams.md` (same directory, gitignored) if absent. The sibling `agents.md`
@-imports `local.agent_teams.md` so anything here overrides or extends shipped team
definitions for this scope.

Most overrides belong in the ROOT `local.agent_teams.md` (one directory up), not here.
Use this file only for .claude/-scope-specific team overrides.

Define teams using the same `###`-heading-per-team format as `agent_teams.md`. A
same-named team here overrides the shipped definition; new names are unioned in.
Keep it short — this file loads into context every session.

See file_structure_invariants.md §13.
-->
